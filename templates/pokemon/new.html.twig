{% extends 'base.html.twig' %}

{% block title %}Crear nuevo Pokémon{% endblock %}

{% block body %}
    <h1>Crear nuevo Pokémon</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">Guardar</button>
    {{ form_end(form) }}

    <a href="{{ path('app_pokemon_index') }}">Volver a la lista</a>
{% endblock %}